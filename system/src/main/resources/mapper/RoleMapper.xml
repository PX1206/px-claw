<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.claw.system.mapper.RoleMapper">

    <select id="getRoleList" resultType="com.claw.system.vo.RoleVO">
        SELECT r.id, r.name, r.code, r.description, r.status, r.create_time,
               (SELECT COUNT(1) FROM user_role ur WHERE ur.role_id = r.id) AS userCount
        FROM role r
        <where>
            <if test="param.keyword != null and param.keyword != ''">
                AND r.name LIKE CONCAT('%', #{param.keyword, jdbcType=VARCHAR}, '%')
            </if>
            AND r.del_flag = 0
        </where>
        ORDER BY r.id ASC
    </select>

</mapper>
